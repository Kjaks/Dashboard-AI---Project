FROM node:20-alpine

WORKDIR /app

RUN npm install -g npm

COPY package*.json ./
RUN npm install

COPY . .

EXPOSE 5173

CMD sh -c "npm run test || echo '⚠ Tests failed, check the output above'; npm run dev -- --host 0.0.0.0"
