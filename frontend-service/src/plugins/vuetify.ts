// src/plugins/vuetify.ts
import 'vuetify/styles'
import { createVuetify } from 'vuetify'

const vuetify = createVuetify({
  // aquí puedes configurar temas, componentes, etc.
})

export default vuetify
